# Python 3.13+ required
# 
# This file is kept for backward compatibility, but the canonical
# dependency list is in pyproject.toml. Install with:
#   pip install -e .
# Or to install just the runtime dependencies:
#   pip install pygame PyYAML pydantic

pygame>=2.6.1
PyYAML>=6.0.2
pydantic>=2.10.5
